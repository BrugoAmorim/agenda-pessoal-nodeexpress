<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editar informações do contato</title>
</head>
<body>
    <form action="/editar-info/{{id}}?method=PUT" method="post">
        {{#each Docs}}
            <p>
                <input type="text" value="{{contato}}" name="contato">
            </p>
            <p>
                <input type="text" value="{{celular}}" name="celular">
            </p>
            <p>
                <input type="text" value="{{telefone}}" name="telefone">
            </p>
            <p>
                <input type="text" value="{{email}}" name="email">
            </p>
            <p>
                <input type="text" value="{{cidade}}" name="cidade">
            </p>
            <p>
                <input type="text" value="{{estado}}" name="estado">
            </p>
            <p>
                <input type="text" value="{{notas}}" name="notas">
            </p>
            <button>salvar</button>
        {{/each}}
    </form>
</body>
</html>